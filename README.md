# Sasidharan Portfolio

Simple static portfolio site containing the following files:

- `index.html` — main HTML page
- `styles.css` — styles
- `script.js` — JavaScript behavior

How to view
-
- Open `index.html` directly in a browser

Notes
-
- Minimal, client-side project — no build step required.
